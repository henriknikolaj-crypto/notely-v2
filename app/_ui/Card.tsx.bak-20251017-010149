import type { ReactNode } from "react";

export default function Card({ className = "", children, style, id }:{ className?: string; children: ReactNode; style?: React.CSSProperties; id?: string }) {
  return (
    <div id={id} style={style} className={`bg-white border rounded-md shadow-sm ${className}`}>
      {children}
    </div>
  );
}

